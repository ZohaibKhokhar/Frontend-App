import { AlphaSpaceOnlyDirective } from './alpha-space-only.directive';

describe('AlphaSpaceOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaSpaceOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
